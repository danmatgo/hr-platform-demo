<% content_for :title, "Editing benefit plan" %>

<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">Edit Benefit Plan</h1>
    <div class="flex items-center space-x-2">
      <%= link_to "Show", @benefit_plan, class: "btn btn-muted" %>
      <%= link_to "Back", benefit_plans_path, class: "btn btn-muted" %>
    </div>
  </div>
  <%= render "form", benefit_plan: @benefit_plan %>
</div>
