<% content_for :title, "New benefit plan" %>

<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">New Benefit Plan</h1>
    <%= link_to "Back", benefit_plans_path, class: "btn btn-muted" %>
  </div>
  <%= render "form", benefit_plan: @benefit_plan %>
</div>
