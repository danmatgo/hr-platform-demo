<% content_for :title, "Editing time entry" %>

<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">Edit Time Entry</h1>
    <div class="flex items-center space-x-2">
      <%= link_to "Show", @time_entry, class: "btn btn-muted" %>
      <%= link_to "Back", time_entries_path, class: "btn btn-muted" %>
    </div>
  </div>
  <%= render "form", time_entry: @time_entry %>
</div>
