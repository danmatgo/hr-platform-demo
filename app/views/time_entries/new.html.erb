<% content_for :title, "New time entry" %>

<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">New Time Entry</h1>
    <%= link_to "Back", time_entries_path, class: "btn btn-muted" %>
  </div>
  <%= render "form", time_entry: @time_entry %>
</div>
