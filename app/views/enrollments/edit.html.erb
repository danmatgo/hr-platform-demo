<% content_for :title, "Editing enrollment" %>

<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">Edit Enrollment</h1>
    <div class="flex items-center space-x-2">
      <%= link_to "Show", @enrollment, class: "btn btn-muted" %>
      <%= link_to "Back", enrollments_path, class: "btn btn-muted" %>
    </div>
  </div>
  <%= render "form", enrollment: @enrollment %>
</div>
