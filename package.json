{
  "name": "hr-platform-demo",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "playwright:install": "playwright install --with-deps"
  }
}
